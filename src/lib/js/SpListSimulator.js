class SpListSimulator{
    constructor(GUID = ["id", "firstName", "lastName"]){
        this.list = GUID;
        this.cols = {};
        GUID.forEach(item => {
            const value = null,
            const setValue = (val)=>{value = val};
        });
        
    }
}